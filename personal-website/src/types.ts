// src/types.ts
export interface Publication {
    title: string;
    conference: string;
    link: string;
  }
  
  export interface Project {
    title: string;
    description: string;
    tech: string[];
  }